name: Atlas regression checks

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install development dependencies
        run: npm install --ignore-scripts
      - name: Data and inventory regressions
        run: npm test
      - name: Build all themes and PR review demo
        run: |
          mkdir -p work/browser-smoke
          python3 scripts/build.py examples/order-flow.data.json work/browser-smoke/atlas-studio.html
          python3 scripts/build.py examples/order-flow.data.json work/browser-smoke/atlas-foundry.html themes/foundry.css
          python3 scripts/build.py examples/order-flow.data.json work/browser-smoke/atlas-drafting.html themes/drafting.css
          python3 scripts/build.py examples/review-demo.data.json work/browser-smoke/atlas-review.html
          node tests/review-fixtures.js
      - name: Install Chromium for browser checks
        run: npx playwright install --with-deps chromium
      - name: Browser smoke tests
        run: npm run test:browser
      - name: PR review browser tests
        run: node tests/review-browser.js
      - name: Save browser examples and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: atlas-browser-smoke
          path: work/browser-smoke
          if-no-files-found: ignore
